version: 3
projects:
- dir: infrastructure/core/vpc
  workflow: default
  autoplan:
    enabled: true
    when_modified: ["*.tf", "*.tfvars"]
- dir: infrastructure/core/eks
  workflow: eks
  autoplan:
    enabled: true
    when_modified: ["*.tf", "*.tfvars"]
